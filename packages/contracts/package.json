{
  "name": "@draftpay/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build --sizes",
    "test": "forge test -vv",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "deploy:arc": "forge script script/DeployArc.s.sol:DeployArc --rpc-url arc_testnet --account draftpay-deployer --broadcast --verify",
    "clean": "forge clean"
  }
}
